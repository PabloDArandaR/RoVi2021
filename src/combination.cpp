int main()
{
    // Detect the object

    // Find the pose

    // Obtain the frame of the object based on the frame of the camera and the pose detected

    // Use the frame obtained to choose the first intermediate point of the path.

    // 1 intermediate point more

    // end-approximation points
    return 0;
}